[
  {
    "epic_id": "vulture-test-cleanup",
    "phase_id": "phase-01-fix-redundant-ifs",
    "module_scope": "tests/providers",
    "instructions": "Vulture has flagged 11 'redundant if-condition' warnings across `test_markdown.py` and `test_tree.py`. Inspect these files, identify the redundant conditions (e.g., `if True:`, static truthy evaluations, or statically matching strings), and refactor them so the test assertions test the actual provider outputs. Run `uv run vulture src/ tests/ .vulture_whitelist.py` to confirm zero output.",
    "context_targets": [
      {
        "provider_name": "file",
        "target_identifier": "tests/providers/test_markdown.py",
        "is_required": true,
        "resolution_mode": "FULL_SOURCE"
      },
      {
        "provider_name": "file",
        "target_identifier": "tests/providers/test_tree.py",
        "is_required": true,
        "resolution_mode": "FULL_SOURCE"
      }
    ],
    "anti_patterns": [
      "Do not delete the tests themselves. Only fix the redundant `if` logic.",
      "Do not add `# vulture: ignore` comments. Fix the root cause.",
      "Ensure you do not drop test coverage. The assertions must remain robust."
    ],
    "completion_criteria": [
      "Both test files pass Pytest.",
      "Vulture returns completely empty output.",
      "Test coverage remains at 100%."
    ],
    "verification_commands": [
      "uv run vulture src/ tests/ .vulture_whitelist.py",
      "just verify"
    ]
  }
]